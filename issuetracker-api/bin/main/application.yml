spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/test
    username: comento_main
    password:
  mvc:
    path match:
      matching-strategy: ant_path_matcher